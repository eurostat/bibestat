name: Automated JSON bibliography creation from BibTeX file

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: BibTeX to BibJSON step
      uses: ./.github/actions/estatbib2json
      with:
        bibtex-fname: 'eurostatref'